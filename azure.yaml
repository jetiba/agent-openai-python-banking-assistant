# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: aca-workshop
metadata:
  template: aca-workshop

services:
  account:
    project: ./app/business-api/python/account
    host: containerapp
    language: python
    docker:
      path: ./Dockerfile
